#include <stdio.h>
#include <raylib.h>

int main(){
    InitWindow(WIDTH, HEIGHT, "Double Pendulum");

    while(!WindowShouldClose){
        BeginDrawing();

        EndDrawing();
    }
    return 0;
}

